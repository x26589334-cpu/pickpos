# 📌 픽포스(PickPOS) 웹사이트 작업노트

> 다른 컴퓨터에서 이어서 작업할 때 Claude에게 "작업노트 읽어줘"라고 하면 됩니다.
> 사용자는 코딩 초보자이므로 안내는 버튼 하나하나 천천히.

## 🧩 기본 정보
- **사이트 성격**: 픽포스 — 포스기·카드단말기·키오스크·테이블오더·무인자판기 **비교 견적** 랜딩 페이지
- **기존 사이트(hpos.co.kr, 리포 `yoon`)와의 차이**: 같은 상품을 다루지만 완전히 다른 사이트
  - hpos = 지역(동 단위) 검색 중심, 초록색, "설치비 무료·평균 4일·24시간 AS" 메시지
  - 픽포스 = **장비 진단 → 구입·렌탈 36개월 비용 계산 → 견적 신청** 흐름, 네이비+오렌지, Pretendard
  - 문구·섹션·디자인 전부 새로 작성. 상품 사진 6장만 hpos에서 가져옴 (images/)
- **브랜드명**: 픽포스 (PickPOS) — 원하면 index.html에서 일괄 치환
- **도메인**: https://hsupporter.com/ (구입 완료, CNAME 파일 있음). 가비아 DNS: A레코드 4개(@→185.199.108.153 / 109.153 / 110.153 / 111.153) + CNAME(www→x26589334-cpu.github.io.)
- **전화번호**: 010-6832-1994 (hpos와 동일. 다른 번호 쓰려면 index.html·form.js 에서 `01068321994`, `010-6832-1994` 치환)
- **GitHub**: https://github.com/x26589334-cpu/pickpos (Pages 활성화, 커스텀 도메인 hsupporter.com 설정됨 — 2026-08-29)

## 📁 파일 구조
- `index.html` — 페이지 본체 (섹션: 진단 #finder / 카탈로그 #catalog / 계산기 #calc / 절차 #process / 업종 세트 #sets / 교체·철거 #swap / FAQ #faq / 견적 폼 #quote)
- `style.css` — 스타일 (색상은 맨 위 `:root` 변수)
- `script.js` — 진단·탭·계산기 동작. **단가는 맨 위 `PRICE` 객체** (참고용 예시값)
- `form.js` — 견적 폼 → 구글 시트. `SHEET_ENDPOINT` 가 `PASTE_` 그대로면 데모 모드(저장 안 됨)
- `images/` — 상품 사진 (pos, terminal, terminal-bt, kiosk, tableorder, vending)
- `favicon.svg`, `robots.txt`, `sitemap.xml`

## ⚠️ 확인·수정이 필요한 것
1. **금액**: 월 이용료는 hpos 요금표와 동일하게 맞춤 완료(단말기 9,900 / 포스 21,000 / 키오스크 33,000 / 토스페이결제기 11,000 / 테이블오더 14,000). 무인자판기 렌탈 250,000 확정(2026-09-03). 남은 예시값: **구입가 전부** — 실제 단가 나오면 script.js PRICE + index.html 표기 둘 다 수정.
2. **시트 연동**: 현재는 과외와 같은 웹앱 URL이라 "웹 문의" 시트의 **과외 탭**에 구분 "픽포스-견적신청"으로 들어감. **서포트포스 탭으로 분리하려면** apps-script-견적.gs(SHEET_NAME="서포트포스")를 script.google.com 에 새 프로젝트로 배포(웹 앱·모든 사용자) → 웹앱 URL을 form.js SHEET_ENDPOINT 에 교체.
3. 장소 제공형 자판기·중고 매입 모두 실제 제공 (사용자 확인 2026-08-29)
4. **영업시간 09:00–21:00 연중무휴** — hpos 값을 그대로 씀. 다르면 수정.
5. 도메인 구입 후 네이버 서치어드바이저·구글 서치콘솔 등록 (hpos 때와 같은 순서), GA4 새 속성 생성.

## 🚀 배포 상태
- GitHub Pages: 리포 Settings → Pages 에서 main / (root), 커스텀 도메인 hsupporter.com 설정 완료
- 가비아 DNS 설정 후 Pages 화면에서 "Enforce HTTPS" 체크 필요 (DNS 반영 뒤 가능)

## 🔧 작업 방식
- 수정 → `git add -A; git commit -m "내용"; git push` → 1~2분 뒤 반영
- 작업 시작 전 `git pull`, 끝나면 `git push`

## 🎨 디자인 (2026-09-03 리뉴얼)
- hpos의 **완성도**만 참고하고 **색은 픽포스 그대로**(네이비 #14284B + 오렌지 #F25C1F + 골드) 유지 — 두 사이트를 색으로 구분
- 폰트: 세리프(Noto Serif KR) → **Pretendard** 로 전면 교체. `--serif`/`--sans` 변수 둘 다 Pretendard 를 가리키므로, 변수만 바꾸면 한 번에 되돌릴 수 있음
- hpos 에서 가져온 것: 상단 프로모 띠(`.promobar`), 스크롤 시 그림자 생기는 상단바(`.top.is-scrolled`), 스크롤 등장 효과(`.reveal` + IntersectionObserver), 버튼 호버 리프트
- `.reveal` 클래스를 붙인 요소는 화면에 들어올 때 나타남. **새 섹션을 추가하면 `class="... reveal"` 을 같이 붙일 것** (안 붙이면 그냥 처음부터 보임 — 깨지지는 않음)
- 모션 끄기 설정(`prefers-reduced-motion`)한 사용자에겐 애니메이션 전부 비활성

## 📚 비교 가이드 (compare/) — 2026-09-05 시작
- **왜**: H포스(hpos.co.kr)가 「지역+장비+**설치**」 검색어를 700여 페이지로 이미 잡고 있다. 같은 운영자라 겹치면 서로 깎아먹으므로, 픽포스는 **「상권+장비+구입 vs 렌탈 비용」** 으로 축을 다르게 간다.
- **구조**
  - `compare/index.html` — 목록. 새 글은 카드 1개 추가 (최신이 맨 앞)
  - `compare/{slug}.html` — 글 본문. `gangnam-station.html` 이 원본 템플릿
  - `compare/og/{slug}.png` — 글마다 전용 OG 이미지 (사진 대신 **비용 비교 그래픽**)
  - `page.js` — 서브페이지 공용 JS (상단바 스크롤·햄버거·연도·reveal). 메인은 `script.js` 를 쓰고 이건 안 씀
  - `_content/글양식.md` — **매일 글 쓸 때 읽는 지침서**. 구조·키워드 규칙·발행 절차 전부 여기
  - `_content/지역목록.md` — 상권 60곳 + 상권 메모 + 진행 상태
  - `_content/og-template.html` — OG 그래픽 틀 (`{{...}}` 치환 후 헤드리스 크롬으로 렌더)
- **운영**: 사용자가 **"오늘 글 써줘"** 하면 `_content/글양식.md` 대로 하루 1~2편. 360개를 미리 찍지 않는다 (요금 바뀌면 다 고쳐야 하고, 서치콘솔 피드백을 못 씀)
- **함정**
  - `_content/` 는 `_` 로 시작해서 Jekyll 이 게시하지 않는다 (의도한 것). `.nojekyll` 을 만들면 이게 깨지니 **만들지 말 것**
  - `compare/` 안의 페이지는 css·js·favicon 경로가 전부 `../` 다. 임시 파일을 리포 루트에 만들어 렌더하면 CSS 가 안 붙는다 (테스트할 때 임시 파일도 `compare/` 안에 둘 것)
  - 크롬 `--screenshot` 은 **절대경로**여야 한다. 상대경로면 "액세스가 거부되었습니다" 로 실패
  - 요금(`script.js` 의 `PRICE`)이 바뀌면 이미 쓴 `compare/` 글의 숫자도 전부 바뀐다. grep 해서 같이 고칠 것

## 🖼 OG 이미지 (공유 썸네일)
- `og-image.png` (1200×630) — 카톡·네이버·페이스북에 링크 공유할 때 뜨는 썸네일. index.html의 `og:image`/`twitter:image`, JSON-LD `image` 에 연결됨
- **원본은 `og-image.source.html`** — 이 HTML을 헤드리스 크롬으로 찍어서 png로 만든다. 문구·색을 바꾸려면 이 파일만 고치고 아래 명령 재실행:
  ```
  "C:\Program Files\Google\Chrome\Application\chrome.exe" --headless=new --disable-gpu ^
    --window-size=1200,630 --virtual-time-budget=8000 ^
    --screenshot="og-image.png" "og-image.source.html"
  ```
- 이미지를 교체하면 index.html의 `og-image.png?v=` 뒤 날짜를 올려야 카카오·네이버가 옛 썸네일을 계속 쓰지 않는다. 카톡은 별도로 [카카오 캐시 초기화](https://developers.kakao.com/tool/clear/og) 에서 URL 밀어줘야 바로 반영됨
- `og-image.source.html` 은 방문자용 페이지가 아니라서 robots.txt 에서 색인 제외해 뒀다

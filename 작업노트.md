# 📌 픽포스(PickPOS) 웹사이트 작업노트

> 다른 컴퓨터에서 이어서 작업할 때 Claude에게 "작업노트 읽어줘"라고 하면 됩니다.
> 사용자는 코딩 초보자이므로 안내는 버튼 하나하나 천천히.

## 🧩 기본 정보
- **사이트 성격**: 픽포스 — 포스기·카드단말기·키오스크·테이블오더·무인자판기 **비교 견적** 랜딩 페이지
- **기존 사이트(hpos.co.kr, 리포 `yoon`)와의 차이**: 같은 상품을 다루지만 완전히 다른 사이트
  - hpos = 지역(동 단위) 검색 중심, 초록색, "설치비 무료·평균 4일·24시간 AS" 메시지
  - 픽포스 = **장비 진단 → 구입·렌탈 36개월 비용 계산 → 견적 신청** 흐름, 네이비+오렌지, 세리프 헤드라인
  - 문구·섹션·디자인 전부 새로 작성. 상품 사진 6장만 hpos에서 가져옴 (images/)
- **브랜드명**: 픽포스 (PickPOS) — 원하면 index.html에서 일괄 치환
- **도메인(가정)**: https://pickpos.kr/ — 아직 미구입. 실제 도메인 정해지면 index.html(canonical, og:url, JSON-LD), robots.txt, sitemap.xml 의 `pickpos.kr` 을 바꿀 것
- **전화번호**: 010-6832-1994 (hpos와 동일. 다른 번호 쓰려면 index.html·form.js 에서 `01068321994`, `010-6832-1994` 치환)
- **GitHub**: 아직 원격 없음. 생성 방법은 아래.

## 📁 파일 구조
- `index.html` — 페이지 본체 (섹션: 진단 #finder / 카탈로그 #catalog / 계산기 #calc / 절차 #process / 업종 세트 #sets / 교체·철거 #swap / FAQ #faq / 견적 폼 #quote)
- `style.css` — 스타일 (색상은 맨 위 `:root` 변수)
- `script.js` — 진단·탭·계산기 동작. **단가는 맨 위 `PRICE` 객체** (참고용 예시값)
- `form.js` — 견적 폼 → 구글 시트. `SHEET_ENDPOINT` 가 `PASTE_` 그대로면 데모 모드(저장 안 됨)
- `images/` — 상품 사진 (pos, terminal, terminal-bt, kiosk, tableorder, vending)
- `favicon.svg`, `robots.txt`, `sitemap.xml`

## ⚠️ 확인·수정이 필요한 것
1. **금액**: 계산기·카탈로그·세트의 금액은 예시(렌탈 포스 34,000 / 단말기 9,900 / 키오스크 33,000 / 테이블오더 14,000 / 자판기 99,000, 구입가는 추정). 실제 단가로 바꿀 것 — script.js `PRICE` + index.html 표기 금액 둘 다.
2. **폼 연결**: 구글 시트 Apps Script 웹앱 URL을 form.js 에 넣어야 실제 접수됨 (순서는 form.js 상단 주석 참고).
3. **"장소 제공형(위탁)" 자판기 운영, "중고 매입·재정비"** 는 실제로 제공하는 서비스인지 확인. 아니면 해당 문구 삭제.
4. **영업시간 09:00–21:00 연중무휴** — hpos 값을 그대로 씀. 다르면 수정.
5. 도메인 구입 후 네이버 서치어드바이저·구글 서치콘솔 등록 (hpos 때와 같은 순서), GA4 새 속성 생성.

## 🚀 GitHub에 올리기 (처음 한 번)
1. github.com 로그인 → 오른쪽 위 + → New repository → 이름 `pickpos` → Public → Create
2. PowerShell에서:
   ```
   cd $env:USERPROFILE\Projects\pickpos
   git remote add origin https://github.com/x26589334-cpu/pickpos.git
   git push -u origin main
   ```
3. 리포 Settings → Pages → Branch: main / (root) → Save → 1~2분 뒤 https://x26589334-cpu.github.io/pickpos/ 에서 확인
4. 도메인 연결은 hpos 때처럼 `CNAME` 파일 추가 + 가비아 DNS 설정

## 🔧 작업 방식
- 수정 → `git add -A; git commit -m "내용"; git push` → 1~2분 뒤 반영
- 작업 시작 전 `git pull`, 끝나면 `git push`
